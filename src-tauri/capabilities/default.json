{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window: core APIs + native file dialogs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
